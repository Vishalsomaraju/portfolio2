"use client";

// Custom cursor provider removed.
// Kept as a safe stub; no longer mounted in layout.tsx.
export function CursorProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
